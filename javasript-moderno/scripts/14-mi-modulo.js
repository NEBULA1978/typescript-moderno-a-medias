//quiero tener aqui todas las funciones y las
// quierop importar al modulo principal

export default function(datos){
    datos.forEach(element => {
        console.log(element);
    });

    return datos;
};

 